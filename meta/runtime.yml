---
requires_ansible: '>=2.15.0'
# Note: the legacy module names ``cisco.gnmi.cisco_iosxe_gnmi`` and
# ``cisco.gnmi.gnmi`` were removed in 4.0.0. They have been replaced by
# operation-specific modules and there is no redirect:
#   * cisco.gnmi.info          (gNMI Get)
#   * cisco.gnmi.config        (gNMI Set)
#   * cisco.gnmi.subscribe     (gNMI Subscribe)
#   * cisco.gnmi.capabilities  (gNMI Capabilities)
#
# Added in 4.1.0:
#   * cisco.gnmi.gnoi          (gNOI: Certificate Management, OS Installation,
#                              Factory Reset)

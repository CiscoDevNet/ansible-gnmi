---
# Test playbook for Cisco IOS XE gNMI on device 10.85.134.65
- name: Test Cisco IOS XE gNMI Connection
  hosts: localhost
  gather_facts: false

  vars:
    ansible_python_interpreter: "{{ ansible_playbook_python }}"
    device_host: "10.85.134.65"
    device_port: 9339
    device_username: "admin"
    device_password: "your-password-here"

  tasks:
    - name: Test 1 - Get system hostname
      cisco.iosxe_gnmi.cisco_iosxe_gnmi:
        host: "{{ device_host }}"
        port: "{{ device_port }}"
        username: "{{ device_username }}"
        password: "{{ device_password }}"
        operation: get
        encoding: json_ietf
        ca_cert: "/tmp/device_cert.pem"  # Use device's self-signed certificate
        paths:
          - /system/config/hostname
      register: hostname_result
      ignore_errors: true

    - name: Display hostname result
      debug:
        var: hostname_result

    - name: Test 2 - Get interface list
      cisco.iosxe_gnmi.cisco_iosxe_gnmi:
        host: "{{ device_host }}"
        port: "{{ device_port }}"
        username: "{{ device_username }}"
        password: "{{ device_password }}"
        operation: get
        encoding: json_ietf
        ca_cert: "/tmp/device_cert.pem"
        paths:
          - /interfaces/interface
        datatype: config
      register: interfaces_result
      ignore_errors: true
      when: hostname_result is succeeded

    - name: Display interfaces
      debug:
        var: interfaces_result.data
      when: interfaces_result is succeeded
